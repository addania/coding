import React from "react";

export default ({children}) => (
    <div style={{ margin: "3rem auto", maxWidth:650, padding:"0 1rem"}}>
        <h1>Coding journey with Addania</h1>
        {children}
    </div>
)